<%= form_for(@baristum) do |f| %>
  <% if @baristum.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@baristum.errors.count, "error") %> prohibited this baristum from being saved:</h2>

      <ul>
      <% @baristum.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_field :description %>
  </div>
  <div class="field">
    <%= f.label :photo_id %><br />
    <%= f.text_field :photo_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
