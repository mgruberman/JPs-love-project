<% form_for(:image_store, @image_store, :url => {:action=>'create'}, :html=> {:multipart=>true}) do |f| %>
  <% if @image_store.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@image_store.errors.count, "error") %> prohibited this image_store from being saved:</h2>
      <ul>
      <% @image_store.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  
  <%= f.file_field :image_file  %> 
  <%= f.text_field :description %>
 
  <%= hidden_field_tag( :imageType, "user" ) %>
  
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
